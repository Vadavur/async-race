export const AR_GAME = { title: 'Async RACE', name: 'async-race' };

export const BUTTON_NAMES = {
  openGarage: 'garage'.toUpperCase(),
  listWinners: 'winners'.toUpperCase(),
  createCar: 'create'.toUpperCase(),
  updateCar: 'update'.toUpperCase(),
  startRace: 'race'.toUpperCase(),
  resetGarage: 'reset'.toUpperCase(),
  generateCars: 'generate cars'.toUpperCase(),
  selectCar: 'select'.toUpperCase(),
  removeCar: 'remove'.toUpperCase(),
  testCar: 'start'.toUpperCase(),
  stopCarTest: 'stop'.toUpperCase(),
  previousPage: 'prev'.toUpperCase(),
  nextPage: 'next'.toUpperCase(),
};

export const END_RACE_MESSAGE = 'Race is over';
